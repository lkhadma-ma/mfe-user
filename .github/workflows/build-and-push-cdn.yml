name: Build User Project CDN & Update Config Repo

on:
  push:
    paths:
      - 'projects/user/public/**'
    branches:
      - main

jobs:
  build_cdn:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - name: Checkout Angular source code
        uses: actions/checkout@v4

      # ---------------------------------------------
      # Checkout deploy/config repo
      # ---------------------------------------------
      - name: Checkout deploy/config repo
        uses: actions/checkout@v4
        with:
          repository: lkhadma-ma/mfe-user-deploy
          ref: main
          token: ${{ secrets.PAT_GITHUB_TOKEN }}
          path: deploy-repo

      # ---------------------------------------------
      # Determine next semantic version from cdn.yml
      # ---------------------------------------------
      - name: Determine next semantic version
        id: semver
        run: |
          CONFIG_FILE="deploy-repo/cdn.yml"
          if [ ! -f "$CONFIG_FILE" ]; then
            LAST_VERSION="v1.0.0"
          else
            LAST_VERSION=$(grep 'version:' "$CONFIG_FILE" | awk '{print $2}' | tr -d '"')
            [ -z "$LAST_VERSION" ] && LAST_VERSION="v1.0.0"
          fi

          VERSION_NUM=${LAST_VERSION#v}
          IFS='.' read -r MAJOR MINOR PATCH <<< "$VERSION_NUM"
          PATCH=$((PATCH + 1))
          NEW_VERSION="v${MAJOR}.${MINOR}.${PATCH}"
          echo "VERSION=$NEW_VERSION" >> $GITHUB_ENV

      # ---------------------------------------------
      # Build & push Docker image (assets)
      # ---------------------------------------------
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        env:
          IMAGE_NAME: ghcr.io/${{ github.repository }}-cdn
        run: |
          IMAGE=$IMAGE_NAME:${{ env.VERSION }}
          LATEST=$IMAGE_NAME:latest
          export DOCKER_BUILDKIT=1
          docker build -f Dockerfile.build -t $IMAGE -t $LATEST  --build-arg MODE=cdn .
          docker push $IMAGE
          docker push $LATEST

      # ---------------------------------------------
      # Update cdn.yml
      # ---------------------------------------------
      - name: Update cdn.yml
        run: |
          cat > deploy-repo/cdn.yml <<EOF
          app:
            version: "${{ env.VERSION }}"
            name: mfe-user
            registry: ghcr.io/${{ github.repository }}
          EOF

      - name: Commit & push cdn.yml and assets
        working-directory: deploy-repo
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add cdn.yml
          git commit -m "chore: update cdn version to ${{ env.VERSION }}"

          git fetch origin main

          git rebase origin/main || (
            echo "Rebase failed, attempting auto-resolve via merge"
            git rebase --abort
            git pull --rebase origin main
          )

          git push origin main
